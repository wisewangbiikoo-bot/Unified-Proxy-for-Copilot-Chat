# API keys (optional)

Most users put `api_key` in `proxy_configs.json` for each backend. SecretStorage commands are only needed for legacy single-backend mode without a config file.

See **configure-proxy-config** step and **README.zh-cn.md**.
