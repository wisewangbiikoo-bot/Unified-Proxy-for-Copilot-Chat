# GitHub About → Topics: ONE line, SPACE-separated (NO commas).
#
# UI limit is often ~50 characters TOTAL for the whole line (not per tag).
# Each tag alone may be up to 50 chars; the input box is much shorter.
#
# --- Pick ONE line below (length shown) ---

# 39 chars (safe if limit is 50):
vscode-extension github-copilot copilot

# 76 chars (if your UI allows ~100):
vscode-extension github-copilot copilot-chat openai-compatible unified-proxy

# 97 chars (if your UI allows ~100):
vscode-extension github-copilot copilot-chat openai-compatible unified-proxy deepseek-v4
